# Evaluation Sample Documents

Add lightweight demo files with these names before running live evaluation:

- `sample_1.pdf`
- `sample_2.pdf`
- `sample_image.png`

The current `run_eval.py` script uses stub answers, so these fixtures are optional until the evaluator is wired to `/ingest` and `/query`.
